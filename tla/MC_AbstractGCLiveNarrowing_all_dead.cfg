\* POSITIVE: all fields omitted by collect_live_values are dead for the next transition.

CONSTANTS
    DeadFields = {"remainingMatches", "remainingAlts", "remainingTemplates"}

SPECIFICATION Spec

INVARIANT
    TypeOK
    NoFutureTouchFreed
