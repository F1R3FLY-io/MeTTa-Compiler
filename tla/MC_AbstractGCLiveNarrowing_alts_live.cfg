\* NEGATIVE: remainingAlts is omitted by collect_live_values but still future-live.

CONSTANTS
    DeadFields = {"remainingMatches", "remainingTemplates"}

SPECIFICATION Spec

INVARIANT
    TypeOK
    NoFutureTouchFreed
