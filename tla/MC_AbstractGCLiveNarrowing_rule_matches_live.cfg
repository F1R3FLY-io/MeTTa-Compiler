\* NEGATIVE: remainingMatches is omitted by collect_live_values but still future-live.

CONSTANTS
    DeadFields = {"remainingAlts", "remainingTemplates"}

SPECIFICATION Spec

INVARIANT
    TypeOK
    NoFutureTouchFreed
