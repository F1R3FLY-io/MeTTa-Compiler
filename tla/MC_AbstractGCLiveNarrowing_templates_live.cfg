\* NEGATIVE: remainingTemplates is omitted by collect_live_values but still future-live.

CONSTANTS
    DeadFields = {"remainingMatches", "remainingAlts"}

SPECIFICATION Spec

INVARIANT
    TypeOK
    NoFutureTouchFreed
