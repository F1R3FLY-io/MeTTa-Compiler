\* NEGATIVE: Collect reaches the driver and the caller waits, but the driver
\* omits the response send attempt.

CONSTANT
  StoreRequestSender = TRUE
  CarryReplySender = TRUE
  DriverAttemptsReply = FALSE
  CallerWaitsAfterSend = TRUE

SPECIFICATION Spec

INVARIANTS
  TypeOK
  RequestReceiveHasProducer
  ResponseWaitHasProducer
  NoOrphanReplySend
  FireAndForgetDoesNotWait
