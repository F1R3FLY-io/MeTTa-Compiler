\* NEGATIVE: rejected boolean resume. A back-to-back request reasserts
\* GC_REQUESTED and re-blocks a worker whose own cycle ended.
CONSTANTS
  UseGenerationResume = FALSE
  BumpAtEnd = TRUE

SPECIFICATION Spec

INVARIANTS
  TypeOK
  EndedCycleCanResume
