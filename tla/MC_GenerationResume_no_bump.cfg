\* NEGATIVE: generation-gated wait without an end-of-cycle bump. The worker's
\* parked generation never ends, so it cannot resume.
CONSTANTS
  UseGenerationResume = TRUE
  BumpAtEnd = FALSE

SPECIFICATION Spec

INVARIANTS
  TypeOK
  EndedCycleCanResume
