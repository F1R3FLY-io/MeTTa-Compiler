\* NEGATIVE (the known phantom re-park -- non-vacuity vs the already-fixed bug,
\* now exercised under the SATB double rendezvous): the re-park gate keys on the
\* bare generation counter (gen > my) instead of the started cycle. After
\* close_cycle#1 bumps gen to K+1 in the conc_mark window -- BEFORE any driver has
\* committed to K+1 (started still K, gip false) -- the parent phantom-re-parks for
\* gen K+1. worker_park_and_root_in_cycle does not publish (gen==target but no live
\* K+1 driver yet drains it the way a started cycle would), and the parent waits for
\* a cycle K+1 end that depends on the driver, which in turn waits on the parent.
\* The phantom strands the parent. EventuallyParentDone is VIOLATED.
\* NoSweepWhileUnpublished still holds.

CONSTANTS
    SatbDoubleRendezvous = TRUE
    UseStartedGate = FALSE
    BClosure = TRUE
    ResumeNotifyOnReopen = TRUE

SPECIFICATION Spec

INVARIANT
    TypeOK
    NoSweepWhileUnpublished

PROPERTY
    EventuallyParentDone
    StrandedCannotPersist
