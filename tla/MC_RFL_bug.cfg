\* R-FL NEGATIVE: pre-fix non-idempotent push. TLC MUST find a violation of
\* NoDuplicateFreeListEntries by sweeping the same current-segment slot twice
\* without a pop in between.

CONSTANTS
    SLOTS = {0, 1}
    FixApplied = FALSE

SPECIFICATION Spec

INVARIANT
    TypeOK
    NoDuplicateFreeListEntries
