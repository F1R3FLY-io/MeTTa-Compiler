\* R-FL POSITIVE: production free_bit idempotent push.
\*
\* Run:
\*   systemd-run --user --scope -p MemoryMax=8G -p MemorySwapMax=0 \
\*       -p CPUQuota=400% java -cp ~/.tla/tla2tools.jar tlc2.TLC \
\*       -workers auto MC_StoreCentricGC_RFL.tla -config MC_RFL_freebit.cfg

CONSTANTS
    SLOTS = {0, 1}
    FixApplied = TRUE

SPECIFICATION Spec

INVARIANT
    TypeOK
    NoDuplicateFreeListEntries
    FreeBitExact
