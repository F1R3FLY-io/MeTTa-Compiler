\* NEGATIVE: boolean GC_REQUESTED resume can be re-blocked by a back-to-back
\* request before parked workers observe resume.

CONSTANTS
    N = 3
    WorkersContribute = TRUE
    CleanupOnPanic = TRUE
    BumpAtClose = TRUE
    UseGenerationResume = FALSE
    ResumeNotify = TRUE
    BackToBackBeforeResume = TRUE

SPECIFICATION Spec

INVARIANT
    TypeOK
    NoDoneWithParkedWorker

PROPERTY
    EventuallyRendezvousResumed
