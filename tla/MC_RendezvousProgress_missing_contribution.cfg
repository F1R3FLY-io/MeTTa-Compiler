\* NEGATIVE: at least one active participant can fail to park/finish, so the
\* driver can wait forever.

CONSTANTS
    N = 3
    WorkersContribute = FALSE
    CleanupOnPanic = TRUE
    BumpAtClose = TRUE
    UseGenerationResume = TRUE
    ResumeNotify = TRUE
    BackToBackBeforeResume = TRUE

SPECIFICATION Spec

INVARIANT
    TypeOK
    NoDoneWithParkedWorker

PROPERTY
    EventuallyRendezvousResumed
