\* NEGATIVE: a non-reified finisher incorrectly stamps `published_gen`, so
\* the witness can be satisfied before the slot's complete machine is buffered.

CONSTANTS
    Slots = {0, 1}
    StrictPredicate = TRUE
    FinisherStamps = TRUE

SPECIFICATION Spec

INVARIANT
    TypeOK
    RootCompleteOnSweep
