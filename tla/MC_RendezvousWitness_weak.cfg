\* NEGATIVE: buggy acquired>=cur_gen predicate. TLC MUST find a sweep where
\* a current-cycle occupied slot has not buffered roots.

CONSTANTS
    Slots = {0, 1}
    StrictPredicate = FALSE
    FinisherStamps = FALSE

SPECIFICATION Spec

INVARIANT
    TypeOK
    RootCompleteOnSweep
