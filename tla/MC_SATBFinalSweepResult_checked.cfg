\* POSITIVE: a closed final-sweep gate is treated as SATB abort and falls back to STW.

CONSTANTS
    CheckSweepResult = TRUE
    FinalGateOpen = FALSE

SPECIFICATION Spec

INVARIANT
    TypeOK
    FinalSweepHandled
