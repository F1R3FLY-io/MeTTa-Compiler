\* #309/#266 frisbee-drop BUG configuration: QueryGenGuard = FALSE.
\* TLC MUST find a NoDroppedResult violation — a worker tables S partial/empty,
\* the derivation completes, and the consumer is served the frozen partial subset.
CONSTANTS
  Answers = {1, 2}
  QueryGenGuard = FALSE
SPECIFICATION Spec
INVARIANT NoDroppedResult
