\* #309/#266 frisbee-drop FIXED configuration: QueryGenGuard = TRUE.
\* TLC MUST prove NoDroppedResult holds — a consumer reading a stale-generation
\* table entry rejects it and recomputes against the complete derivation, so the
\* full answer set is always served (no frisbee-drop).
CONSTANTS
  Answers = {1, 2}
  QueryGenGuard = TRUE
SPECIFICATION Spec
INVARIANT NoDroppedResult
