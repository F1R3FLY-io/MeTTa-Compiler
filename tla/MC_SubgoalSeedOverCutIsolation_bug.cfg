\* #309/#266 frisbee-drop BUG configuration: PreciseSeeding = FALSE (seed ALL active).
\* TLC MUST find a NoOverCut violation — in the constant state (sIsRecursive = FALSE)
\* the worker seeds and cuts the constant to EMPTY, which is not its sole valid value.
CONSTANTS
  Answers = {1, 2}
  PreciseSeeding = FALSE
SPECIFICATION Spec
INVARIANT NoOverCut
