\* #309/#266 frisbee-drop FIXED configuration: PreciseSeeding = TRUE (seed only recursive).
\* TLC MUST prove NoOverCut holds for BOTH states: the constant (sIsRecursive = FALSE) is
\* never seeded so it derives its full answer set, and the recursion (sIsRecursive = TRUE)
\* is still seeded so it still cuts to its EMPTY fixpoint base.
CONSTANTS
  Answers = {1, 2}
  PreciseSeeding = TRUE
SPECIFICATION Spec
INVARIANT NoOverCut
