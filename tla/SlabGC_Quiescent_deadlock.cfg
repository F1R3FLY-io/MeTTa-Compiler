\* TLC Configuration for SlabGC_Quiescent Liveness and Deadlock Checking
\* Run: cd tla && /usr/bin/tlc MC_SlabGC_Quiescent -workers auto -config SlabGC_Quiescent_deadlock.cfg
\*
\* Uses FairSpec (with weak + strong fairness) and PROPERTY for temporal properties.
\* TLC checks both deadlock freedom and the listed liveness properties.
\*
\* NOTE: Liveness checking is extremely expensive (requires exploring the full
\* state graph AND detecting cycles for each property branch). Constants are
\* minimized to make this tractable — even small increases cause exponential
\* blowup in temporal checking time. Safety invariants are verified with
\* larger constants in SlabGC_Quiescent.cfg and SlabGC_Quiescent_small.cfg.
\*
\* FAIRNESS NOTES:
\*   - CronMonitorPoll has WF (monitor always eventually polls)
\*   - ContinueEval, ThreadDone have SF (may be briefly disabled by gc_acquire)
\*   - SnapshotOK, Abort have SF (may be briefly disabled by EvalGuardEnter)
\*   - SessionGc_TraceAndRelease, DoubleCheckFail have SF (same pattern)
\*   - SessionGc_WaitQuiescent, AcquireFlag, AcquireFail, FreeSession have WF

CONSTANT
    NumEvalThreads = 2
    MaxSlots = 2
    MaxRoots = 1
    MaxExprs = 1
    AllocDeltaThreshold = 1
    MinGcThreshold = 1
    SlotsPerPage = 2
    MaxContextIds = 2

SPECIFICATION FairSpec

\* Safety invariant (also checked during liveness exploration)
INVARIANT
    TypeOK

\* Liveness properties — must eventually hold under fairness
PROPERTY
    AllThreadsComplete
    GcEventuallyTriggered
    BackpressureEventuallyRelaxes
    AllDeadValuesEventuallyFreed
    EmptyPagesEventuallyReleased
    SessionValuesEventuallyFreed
    SessionQueueEventuallyDrained
